apiVersion: v1
kind: Secret
metadata:
  name: gateway-secrets
  namespace: ai-gateway
  labels:
    app.kubernetes.io/name: ai-gateway
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # ⚠️  TEMPLATE — Replace with real values before applying.
  # For production, use Google Secret Manager + External Secrets Operator.
  #
  # To create from CLI instead of this file:
  #   kubectl create secret generic gateway-secrets \
  #     --namespace=ai-gateway \
  #     --from-literal=OPENAI_API_KEY=sk-... \
  #     --from-literal=ANTHROPIC_API_KEY=sk-ant-... \
  #     --from-literal=GOOGLE_API_KEY=AIza... \
  #     --from-literal=OPENAI_EMBEDDING_API_KEY=sk-...
  OPENAI_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  GOOGLE_API_KEY: "REPLACE_ME"
  OPENAI_EMBEDDING_API_KEY: "REPLACE_ME"
