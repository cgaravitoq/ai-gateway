apiVersion: v1
kind: Service
metadata:
  name: ai-gateway-lb
  namespace: ai-gateway
  labels:
    app.kubernetes.io/name: ai-gateway
    app.kubernetes.io/component: loadbalancer
  annotations:
    # GKE-specific: use external Network Load Balancer
    cloud.google.com/l4-rbs: "enabled"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ai-gateway
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
